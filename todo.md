# 待调查事项

## 中文文字边界、行高与设计稿间距还原

状态：待继续调查。本文记录本轮讨论和资料，不代表已验证的通用解决方案。

### 问题与目标

设计工具标出的文字图层边界，与浏览器使用 `line-height` 形成的行框可能不是同一种边界。因此，同样设置图层间距与 CSS `margin`，可见文字之间的空白仍可能不同。

目标是保留多行文案的正常行距，同时让整段文字顶部、底部的边界便于与图片、标题等元素按设计稿对齐。研究方向是裁整首行上方和末行下方的留白，而不是压缩每一行的行高。

### 本轮输入与观察事实

- 页面使用 750 宽设计稿，业务 CSS 按 750 基准自动适配。375 CSS px 宽视口下，设计尺寸约缩放为一半；这一比例来自项目适配规则，并非由设备 DPR 单独决定。
- 主图设计展示尺寸：369 × 314。
- 主标题文案：无权限访问。字体 Microsoft YaHei，字号 45px，字重 400，颜色 `#010101`。
- 副文案：字号 35px，字重 400，颜色 `#696969`，字体同上，固定换行为：

  ```text
  当前账号未被授权。
  如需访问，请向管理员申请权限。
  ```

- 图片到主标题、主标题到副文案的标注间距分别是 12px、30px。标注测量的是图层边界还是可见笔画边缘，尚待确认。
- 用户提供的另一处标题“无法查看文档”截图中，设计工具读数为 `width: 269px`、`height: 45px`、`font-size: 45px`、`line-height: 26px`。这些数值只能确认工具显示了不同的尺寸属性，不能直接证明其文字框就是字形墨迹边界。
- 移动设备未必安装 Microsoft YaHei，字体回退会改变字形及字体度量；实际回退字体尚未确认。

### 概念澄清与尚未验证的尝试

- 字号对应字体 em 尺寸基准，不等于可见汉字的笔画高度；行框、字形可见边界、设计工具的文字图层边界需要分别讨论。
- `line-height: 1` 让指定行高等于字号，但不保证行框贴合可见字形；多行文案可能显得拥挤，不能作为统一解法。
- `line-height: normal` 依赖字体与浏览器，不保证跨设备、跨字体一致，也不自动消除段落外围留白。
- 本轮先后尝试过 8px / 17px 的间距补偿、1.2 / 1.5 的比例行高、统一行高 1、恢复 normal。这些调整没有完成设计稿与实际渲染的视觉对照，不应沉淀为经验常量。
- 截至本轮记录，标题 54px、副文案 52px 的行高仅是临时排版基线；外间距保留 12px / 30px。上述行高并非从设计稿确认的最终值，也未完成视觉校准。
- 当前证据不足以认定设计稿不规范，也不足以保证仅靠前端小幅补偿即可跨字体精确还原。

### 通用分析：间距校准受哪些因素影响

间距需先区分**几何间距**（CSS 盒子边缘之间的距离）与**视觉间距**（文字笔画、图片图案等可见内容边缘之间的距离）。设计稿和浏览器若采用不同测量基准，即使 CSS `margin` 等于标注值，观感也可能不一致。

| 因素 | 对校准的影响 |
|---|---|
| 设计稿测量基准 | 标注可能取图层框、文字框或可见内容边缘；应先确认标注两端实际落在哪里。 |
| 字体度量与渲染 | `font-size` 不等于可见字高；字体、字重、字形、系统字体回退和浏览器渲染都会改变笔画在行框中的位置。 |
| 行高与换行 | `line-height` 决定行框及多行之间的距离；`normal` 的实际高度依赖字体与环境。单行调好的补偿值未必适用于多行。 |
| 图片内留白 | 透明或与背景同色的像素仍占图片盒子；图片盒边缘不一定是图案的可见边缘。 |
| 盒模型与布局 | `margin`、`padding`、`gap`、边框、固定高度、对齐方式、定位及垂直 margin 折叠都会影响测得的盒间距。 |
| 缩放与取整 | 设计稿到 CSS 的适配比例、视口和浏览器缩放、小数像素取整会带来差异；设备 DPR 本身不能替代设计稿与 CSS 单位的换算。 |
| 内容变化 | 文案长度、动态数据与窄屏换行可能改变元素高度，需覆盖实际内容，而非仅校准占位文本。 |

当设计稿标的是**可见内容间隙**时，可以用 `目标视觉间隙 − 上一行框底部留白 − 下一行框顶部留白` 估算初始 `margin`；若标的是**盒子间隙**，则不应额外扣除这两处留白。字号只能提供估算，不能当作字形可见高度的实测值。

建议按以下顺序排查：先统一设计稿与 CSS 的单位并确认测量边界；再用 DOM 测量盒子尺寸、计算样式和盒间距；接着检查实际字体、换行及图片内部留白；最后在目标渲染环境中核对视觉边缘并做少量微调。`getBoundingClientRect()` 测得的是盒子，不能单独证明字形笔画边界已对齐。若需跨设备稳定复现，应明确字体资源与行高规则，并验证目标环境，而非沉淀某一设备的固定补偿常量。

### 资料索引

1. [Chrome 官方：CSS text-box-trim](https://developer.chrome.com/blog/css-text-box-trim?hl=zh-cn)
   - 原生 CSS 裁整文字段落外围留白的方案；可保留内部行距。
   - 常见示例 `text-box: trim-both cap alphabetic` 使用拉丁大写高度和基线边界，不能直接视作中文精准裁整方案。
2. [MDN：text-box-trim](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-box-trim)
   - 查属性语义、示例及兼容性入口。后续应按目标浏览器版本核查，不把桌面浏览器支持等同于钉钉 WebView 支持。
3. [CSSWG：CSS Inline Layout Level 3 — Text Edges](https://drafts.csswg.org/css-inline-3/#text-edges)
   - 调查字体度量边界与中文相关边界定义。草案定义不等于浏览器已经实现。
4. [Chromium 初始实现讨论](https://groups.google.com/a/chromium.org/g/blink-dev/c/4NeTpgsi64A/m/hDMkvOs2AQAJ)
   - 初始实现曾暂缓 `ideographic` / `ideographic-ink`。这是历史说明，不能据此断言当前版本仍不支持，需重新核查。
5. [开源 Capsize：seek-oss/capsize](https://github.com/seek-oss/capsize)
   - 依据字体度量生成 CSS，支持配置行高或 `lineGap`；以 `capHeight`、基线等度量为核心，不限于 React 使用。
   - 需调查中文字体度量、混排与系统回退字体的局限，不能作为零配置万能方案直接引入。
6. [开源示例：jantimon/text-box-trim-examples](https://github.com/jantimon/text-box-trim-examples)
   - 用于理解和比较段落外围裁整与压缩每行行高的区别；示例表现不替代目标环境验证。

### 后续待办

- [ ] 确认设计工具名称、导出版本和缩放倍率，查明文字图层 `height` 与 `line-height` 的语义。
- [ ] 确认 12px / 30px 的测量边界，获取副文案实际多行行高；不要依据字号猜字形高度。
- [ ] 核对设计字体及目标设备的实际回退字体，明确跨设备一致性的可接受范围。
- [ ] 检查图片是否带透明或白色内边距；图片文件边界与可见图案边界也可能不同，应独立排查。
- [ ] 查明 `text-box-edge` 的中文边界取值、实现状态，以及目标钉钉 WebView 内核版本的支持情况。
- [ ] 研究 `@supports` 渐进增强及旧内核 fallback，明确不支持裁整时的可接受表现。
- [ ] 调查 Capsize 的中文字体数据、混排及字体回退限制，判断是否值得引入，而非直接套用拉丁示例。
- [ ] 后续获得用户指示后，再对照设计稿验证单行、多行、窄屏换行及不同字体；验证前不宣称视觉还原已完成。
- [ ] 调查收敛后，记录最终方案、适用边界、兼容策略和验证证据，再决定是否提炼为可复用资产。

### 当前执行边界

用户要求停止 computer-use 实机调试验证，先做资料整理和文档调研。本轮仅维护此待办；不继续调整业务页面、不新增自动巡查、不提交 Git。未来验证方式遵循用户后续指示。
